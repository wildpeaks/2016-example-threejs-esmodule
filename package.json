{
  "private": true,
  "name": "example",
  "description": "Example application that uses Rollup and the ES Module bundle of three.js",
  "engines": {
    "node": ">=6.9.0"
  },
  "scripts": {
    "build": "rollup -c || true"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.16.0",
    "rollup": "^0.36.3",
    "rollup-plugin-babel": "^2.6.1",
    "rollup-plugin-node-resolve": "^2.0.0",
    "three": "github:wildpeaks/three.js#dev"
  }
}
